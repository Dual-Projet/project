// factory function to upload cources to our plateform
function generateCourses(name,src,category){
    return {
        name:name,
        src:src,
        category:category
    }
}
// add is a function wich allow us to add courses to our platform
function add(){

}
